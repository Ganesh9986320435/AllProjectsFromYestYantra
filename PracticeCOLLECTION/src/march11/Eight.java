package march11;

import java.util.Arrays;

public class Eight {
	public static void main(String[] args) {
		int a[]= {0,2};
		int b[]=Arrays.stream(a).sorted().toArray();
		for(int i=0;i<=b[b.length-1]+1;i++)
		{
			for(int j=0;j<a.length;j++)
			{
				if(a[j])
			}
		}
	}
}
